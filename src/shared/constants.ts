export const apiUrl = import.meta.env.VITE_API_URL;

export const linksUrl = `${apiUrl}/links`;

export const adminLinksUrl = `${apiUrl}/admin/links/all`;

export const adminLinksManagementUrl = `${apiUrl}/admin/links`;

export const userUrl = `${apiUrl}/users`;

export const loginUrl = `${apiUrl}/auth/login`;

export const signupUrl = `${apiUrl}/users`;
